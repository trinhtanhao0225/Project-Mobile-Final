<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Nền trắng -->
    <corners android:radius="10dp"/> <!-- Góc bo tròn -->
    <stroke
        android:color="@android:color/black"
    android:width="2dp"/>
</shape>
